services:
  - type: web
    name: market-desk
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /market/overview
    envVars:
      - key: JAVA_OPTS
        value: -XX:MaxRAMPercentage=75.0
