# api

REST helpers for the backend's auth and market-overview endpoints (`POST /auth/login`
and `GET /market/overview`). Requests go to same-origin paths that the Vite dev server
proxies to the backend.
