# views

The three top-level screens of the app: Login, Market Overview, and Order Book.
Each file here is a route-level component; shared state and the session socket are
owned above these views, not created inside them.
