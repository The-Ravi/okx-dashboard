/**
 * Adapter for the OKX upstream: REST ticker polling and the shared public WebSocket client.
 * These types implement application ports in {@code com.assignment.marketdata.services}; they are
 * not called directly from controllers or the session WebSocket handler.
 */
package com.assignment.marketdata.httphandlers;
