/**
 * Shared in-process helpers that are not session state, HTTP adapters, or application services:
 * constants and utility methods used across the backend.
 */
package com.assignment.marketdata.utility;
