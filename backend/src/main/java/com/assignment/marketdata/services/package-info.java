/**
 * Application services. Controllers and the session gateway call into this package; it is the only
 * layer that talks to the session store and the OKX HTTP adapter on their behalf.
 */
package com.assignment.marketdata.services;
